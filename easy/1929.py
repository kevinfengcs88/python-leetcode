class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        # given a list of ints, return a concatenated array that consists of the original list with another appended to the end
        return nums + nums
